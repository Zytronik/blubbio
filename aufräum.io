animations:
sprites in on start generieren?
singleton animations
cancel animation (ids)
secondary animation trigger

container.ts
ablauf von allen manager von menu zu game
grafik, container, daten, input, animationen, countdown, timer

back button